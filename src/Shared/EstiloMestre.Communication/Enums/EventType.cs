namespace EstiloMestre.Communication.Enums;

public enum EventType //janela de trbalho
{
    WorkPeriodStart, //(abre)
    BookingStart,  //(fecha)
    BookingEnd, //(abre)
    WorkPeriodEnd //(fecha)
}

